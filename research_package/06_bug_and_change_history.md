# Bug and Change History

This document traces the historical changes, software versions, and resolved or active bugs identified through code archaeology, changelogs, and database checks.

---

## 1. Version Chronology and Development Clues

The codebase shows a development transition from a multi-broker crypto/futures spot bot to a lean, single-broker Kalshi weather bot.

*   **Version Reference**: `v19.9.8` (June 8, 2026) / `v19.1.12` / `v18.32` (Legacy transition).
*   **Active Runtime lane**: `forecast` (Kalshi weather).
*   **Archived lanes**: Crypto, spot, stocks, futures, and legacy research surfaces (removed on June 4, 2026).

---

## 2. Documented Bug Fixes (Confirmed via Changelog)

The following bugs were resolved in previous update cycles:

### 2.1 The ISO Expiry Parsing Bug (2026-06-04)
*   *Symptom*: Valid contracts were getting falsely blocked by `RESOLUTION_HORIZON_TOO_SHORT`.
*   *Cause*: `forecast.strategy_engine._hours_to_resolution()` parsed ISO Kalshi expiry timestamps incorrectly, resulting in `0.0` hours remaining.
*   *Fix*: Hardened string parsing to handle timezone offsets (`Z` and `+` indicators) properly.

### 2.2 Cockpit Realized P&L Truncation (2026-06-08)
*   *Symptom*: Realized P&L box on dashboard was displaying incorrect rolling totals.
*   *Cause*: Totals were calculated only from the last 25 recent trades.
*   *Fix*: Replaced calculations with a database-backed total (`total_won + total_lost`) to reflect complete session P&L.

### 2.3 Flat Fee Assumption Inaccuracy (2026-06-06)
*   *Symptom*: Discrepancies in expected value (EV) and sizing computations.
*   *Cause*: The bot assumed a flat `$0.07` fee per contract.
*   *Fix*: Implemented the exchange-derived Kalshi fee model, unifying cost calculations across strategy engine, cockpit, and broker.

### 2.4 Deploy-Time Logs Folder Permission Errors (2026-06-06)
*   *Symptom*: Deployment failures due to file system permission blocks on log files.
*   *Cause*: Root-owned log file conflicts generated by Docker container runs.
*   *Fix*: provisional release artifacts are written through a one-shot container using standard mounting permissions.

---

## 3. Active Software Anomalies and SRE Risks

These issues are present in the current live environment:

### 3.1 unit Test Pollution of Watermarks (SRE Risk)
*   *File affected*: [`logs/weather_watermarks.json`](file:///Users/joshmacbookair2020/Projects/algo_trading_final/logs/weather_watermarks.json)
*   *Description*: The unit test suite (`tests/proof/test_weather_intraday_watermarks.py`) executes using the production configuration settings. This causes tests to write mock observed temperatures (`74.0`) directly to the production cache.
*   *Impact*: Corrupts observed temperature boundaries. If the bot evaluates real-time intraday exits (which look up observed highs/lows from watermarks), it will trade/exit on corrupted data.

### 3.2 Database Timing Mismatch (Execution Sync Bug)
*   *Tables affected*: `trades` vs `forecast_positions` in [`logs/trades.db`](file:///Users/joshmacbookair2020/Projects/algo_trading_final/logs/trades.db).
*   *Description*: Active positions are logged with an entry price of `0.35` USD, while the `trades` table logs actual fills at `0.01` and `0.07` USD.
*   *Impact*: The position reconciler was executed immediately after order placement. Because the database trade record was not committed yet or could not be matched at that exact millisecond, the reconciler fell back to the order book's `mid` price ($0.35) and permanently cached it.

### 3.3 Resolved: Adaptive-weight production disconnect
*   *Historical description*: An older, uncalled weather learner coexisted with the production pricing path, while a later pricing loader referenced `DB_PATH` without importing it, causing a swallowed exception and 60/40 fallback.
*   *Resolution (deployed v19.20)*: The disconnected learner and its dead schema were removed. `forecast.pricing_engine.calculate_brier_weights()` now reads only explicitly promoted RBI 2.0 champion weights for the deterministic GFS/ECMWF blend. ICON and the commercial ensemble path are excluded.
