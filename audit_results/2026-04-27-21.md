╔══════════════════════════════════════════════════════════════╗
║  SELF-AUDIT v18.7 · 2026-04-27 21:10 ET · LIVE MODE         ║
║  Bot: ●ALIVE (PID 76046) · HB: ~40s · Positions: 1 · P&L: — ║
╚══════════════════════════════════════════════════════════════╝

■ AUTO-REMEDIATED (3 items — no action needed)
  1. CLAUDE.md version corrected: v17.5 → v18.7 (matched AGENTS.md / validate.py)
  2. CHANGELOG entries added for 2 recent fix batches (config perp+depth+targetR, XRP limit order)
  3. XRP/SOL spot limit order 400 error fixed: removed `quote_size` from `limit_limit_gtc` body
     (Coinbase protobuf oneof — setting both quote_size AND base_size caused silent 400 on every
     limit order; bot restarted to load fix at PID 76046)

■ WORK QUEUE — 8 items

  🔴 BLOCK [SAFETY + ENGINEERING]  [RECURRING — fix overdue]
  Disk at 97% full — one large log flush from fatal write failure
  Evidence: `df -h` → /dev/disk3s1 228Gi total, 173Gi used (97%). logs/ = 1.9GB. bot.log = 11MB.
  Impact: SQLite WAL write failure → open_positions table corrupt → bot halts mid-session.
            Previous audit flagged this; bot.log has grown ~1MB since. Disk crossed 97% mark.
  Fix:   (1) `find logs/ -name "*.log.gz" -o -name "bot_error.log.gz"` — compress old logs.
            (2) `> logs/bot.log` to truncate (safe with WAL mode; bot re-opens on next write).
            (3) `launchctl start com.algotrading.backup` then `bash scripts/backup_db.sh`.
            (4) Long term: add logrotate rule max 50MB keep 3.
  Verify: `df -h logs/` shows <90%. `ls -lh logs/bot.log` shows <20MB.

  🔴 BLOCK [REVENUE]  [RECURRING — fix overdue]
  Zero live trades entered in 7 days of continuous scanning
  Evidence: scan_funnels 7d: 10,233 scanned → 0 entered across every single day.
            Current live blockers (post-restart, WAL-fresh): BTC=econ_veto(projected_net_win_too_small),
            PF_ETHUSD=econ_veto(depth_below_minimum), XRP/SOL=spot_entry_failed (FIXED this audit).
            No live closed P&L. Account static at $1,909.66.
  Impact:  $0 revenue. Every day of zero entries is a day the ML, Bayesian, and labeling pipelines
            receive zero signal to learn from. The system is in total stasis.
  Fix:    See items below for per-blocker fixes. The XRP/SOL limit-order fix just deployed.
            Remaining: BTC risk_fraction too small → position too small → net_win < threshold.
            Increase BTC risk_fraction from 0.001 to 0.0018 in SPOT_SCALP_SYMBOL_CONFIG.
            ETH depth: PF_ETHUSD perp is failing — route ETH LONG to spot instead (already configured
            in tradeability engine; verify routing is actually hitting coinbase_spot_broker not perp).
  Verify: `SELECT SUM(entered) FROM scan_funnels WHERE date(ts) = date('now')` > 0.

  🟠 HIGH [REVENUE]  [RECURRING — fix overdue]
  Bayesian signal weights ALL saturated at cap (4×–25×) — total discrimination loss
  Evidence: Agent D: every single signal flagged DOMINANT (>1.80). macd_consensus=25×,
            wavetrend_cross=12×, supertrend_cross=12×, ichimoku=8×. No signal is suppressed.
            The cap documented in CLAUDE.md (2.5× original prior pts) is either not enforced
            or the prior point values are so small that 2.5× = a large absolute multiplier.
  Impact:  Technical tower is running on fixed weights — equivalent to equal-weighting all signals.
            Signals that historically fail are getting 8-25× amplification alongside signals that work.
            This is directly contributing to the 0% live trade win rate.
  Fix:   `learning/dynamic_weights.py` — add a hard cap of `max(weight, prior_weight * 2.5)` where
            prior_weight is the base point value from signal_engine, not 1.0. Or reset weights DB table
            to clear accumulated drift: `DELETE FROM signal_performance WHERE n_fires < 10`.
  Verify: `get_all_weights()` returns values ≤ 2.5× the signal's base technical_score contribution.

  🟠 HIGH [REVENUE]  [RECURRING — escalated from MEDIUM]
  ML tower Pearson r=0.000 — pure noise contributing to every entry decision
  Evidence: Agent D: r=0.000 on n=300 live matched scan↔trade pairs. Models are 14d old.
            ML score distribution: avg=41.7 (below 50 neutral), range=[0,89.5], flat-band(45-55)=24%.
            ml_feature_snapshots table returned 0 rows — live training data collection may be broken.
  Impact:  ML = 20% of composite (80/20 blend). Every composite score is partially randomized.
            With ML stuck at noise, the signal engine is effectively a 1-tower system running at 80%.
            Estimate: if ML contributed genuine signal (+0.2 Pearson), composite gate would filter
            ~15% more false positives → ~15% fewer wasted econ-gate computations.
  Fix:    (1) Diagnose why ml_feature_snapshots table is inaccessible (sys.path issue in learning_loop).
            (2) Once live trades start entering (see BLOCK above), retrain models after 30+ closes.
            (3) Short term: reduce ML weight from 20% to 5% until retrained. Config:
            `SPOT_SCALP_SCORE_WEIGHT_COMPOSITE=0.95, SPOT_SCALP_SCORE_WEIGHT_DERIVATIVE=0.05`
            and reduce ML tower weight in signal_engine.py composite formula.
  Verify: `SELECT COUNT(*) FROM ml_feature_snapshots WHERE source='live_v10'` > 30. Retrain.
            Pearson r > 0.15 on next audit.

  🟠 HIGH [SAFETY]  [RECURRING — escalated from MEDIUM]
  No DB backups exist — single point of failure
  Evidence: Agent A: `ls logs/backups/` returns nothing. `com.algotrading.backup` launchd shows
            PID `-` (not started). backup.log shows it ran Apr 27 02:00 but created no backup files.
            backup_db.sh may be writing to wrong path or silently failing.
  Impact:  If disk fills → WAL write fails → DB corrupts → all open_positions, Bayesian weights,
            integrity tiers, and 7d of live scan history lost permanently. No recovery path.
  Fix:    `bash scripts/backup_db.sh` manually, then `ls -lh logs/backups/` to verify file appears.
            If backup path wrong, `grep -n "BACKUP\|mkdir\|cp " scripts/backup_db.sh` to diagnose.
  Verify: `ls -lth logs/backups/ | head -3` shows a file created today. Then `launchctl start
            com.algotrading.backup` to restore automated nightly backup.

  🟠 HIGH [REVENUE]
  BTC econ_veto: projected_net_win_too_small — best BTC signals always blocked
  Evidence: Post-restart WAL scan: BTC comp=56.3, dec=econ_veto, blk=projected_net_win_too_small,
            rr=0.141 on every cycle. Math: BTC risk_fraction=0.001, account=$1,909 →
            risk=$1.91, stop=0.8%, position=$238, target=1.5×0.8%=1.2%, gross_win=$2.86,
            fees=1%×$238=$2.38, net_win=$0.48. Economics gate rejects because $0.48 < min threshold.
  Impact:  BTC is the highest-quality signal source (WAE Momentum Explosion Long composite=56.3
            consistently). It passes all gates except the absolute USD win floor. $0 BTC revenue.
  Fix:    Increase BTC `risk_fraction` in SPOT_SCALP_SYMBOL_CONFIG from 0.001 → 0.0018.
            New math: risk=$3.44, position=$430, net_win = $5.16 - $4.30 = $0.86. Should clear gate.
            OR lower the `projected_net_win_usd` minimum threshold in spot_economics_gate.py.
  Verify: `SELECT decision FROM scan_candidates WHERE symbol='BTC' ORDER BY ts DESC LIMIT 5`
            shows econ_veto gone. BTC entering scans with net_rr > 0.

  🟡 MEDIUM [REVENUE]
  PF_ETHUSD perp depth_below_minimum — ETH perp blocked by depth gate intermittently
  Evidence: Post-restart: PF_ETHUSD econ_veto depth_below_minimum on ~40% of cycles (rr=0.28
            but fails depth). The depth_min_usd for ETH spot was lowered to $3K but this is the
            PERP path (PF_ETHUSD goes to perp, not spot). Perp depth threshold not in SPOT_SCALP_SYMBOL_CONFIG.
  Impact:  ETH is frequently in LONG setup (comp=54.4) but blocked. Perp depth gate may be in
            risk/economics_gate.py with a different threshold.
  Fix:    `grep "_MIN_DEPTH\|depth_min\|bid_depth" risk/economics_gate.py | head -10` to find
            the perp depth threshold. Lower from current value to $3,000 to match spot settings.
  Verify: `SELECT decision FROM scan_candidates WHERE symbol='PF_ETHUSD' ORDER BY ts DESC LIMIT 5`
            shows depth_below_minimum gone.

  🟡 MEDIUM [ENGINEERING]
  RBI research loop has never run — zero challengers in pipeline
  Evidence: Agent A: `RBI research: NEVER`. challenger_state table empty. rbi_research source
            has 0 events in system_events. The nightly audit calls the labeler and audit checks
            but does NOT trigger rbi_research_loop.py.
  Impact:  No new strategy families being researched or incubated. The system can only trade
            with hardcoded setup families (impulse_continuation, pullback_reclaim,
            compression_breakout) and cannot discover better parameter combinations via RBI.
  Fix:    `python3 rbi/research_loop.py` manually to trigger first run. Then wire into
            nightly_audit.py or a separate launchd job (30-min schedule).
  Verify: `SELECT source, ts FROM system_events WHERE source='rbi_research' ORDER BY ts DESC LIMIT 1`
            returns a row with today's timestamp.

■ LIVE CALIBRATION SNAPSHOT (from code — not from docs)
  Composite/derivative blend     0.80 / 0.20          (config.py SPOT_SCALP_SCORE_WEIGHT_*)
  Composite entry floor          25.0                 (v10_runner.py _TIER1_COMPOSITE_FLOOR)
  Target R by regime             TREND=2.0 / NEUTRAL=1.5 / CHOP=1.2  (config.py)
  Spot maker wait                6s                   (config.py SPOT_MAKER_WAIT_SECONDS)
  Kill switch floor (live)       50% of live baseline ($955 on $1,909 account)
  Max spot deployed              50%                  (config.py SPOT_MAX_DEPLOYED_PCT)
  Depth minimums (all symbols)   BTC/ETH/SOL/XRP=3000, LTC/DOGE/ADA=2000, LINK=1500 USD
  AUTONOMOUS_LIVE_PERP_SYMBOLS   BTC,ETH,SOL,XRP      (config.py)
  BTC risk_fraction              0.001                (SPOT_SCALP_SYMBOL_CONFIG — too small, see HIGH item)
  SOL risk_fraction              0.0018               (SPOT_SCALP_SYMBOL_CONFIG)
  ML models age                  14 days              (ml/models/*.pkl)
  ML score avg (7d candidates)   41.7 (range 0–89.5)
  Econ gate tier B EV            0.05%                (risk/economics_gate.py _TIER_B_EV)

■ METRICS SNAPSHOT
  Funnel 24h:     2,560 scanned → 2,315 threshold → 756 econ → 0 entered (0.0% conversion)
  Win rate:       0% this week (0 closes) vs 0% last week (2 closes, $0 net) — NO DATA
  Maker rate:     0% (0 spot trades ever filled) [target >55%]
  Fee drag:       $0.35 across 2 perp closes = $0.18/trade avg — no spot data yet
  ML signal:      r=0.000 (NOISE) — no predictive value
  Top blocker:    spot_entry_failed / spot_min_order_not_met (394+145=539 kills, 27% of blocks, FIXED)
  Active blockers now: BTC=projected_net_win_too_small, ETH=depth_below_minimum, SOL/XRP=FIXED
  Spot regimes:   unavailable standalone (requires live runtime state from spot_momentum.py)
  Bayesian:       ALL 52 signals DOMINANT (4×–25×) — zero discrimination

  ⚠ ZERO REVENUE 7 CONSECUTIVE DAYS — critical stasis

■ STORED → audit_results/2026-04-27-21.md · Memory updated
